#!/usr/bin/env bash
# Deprecated: the API no longer lives in this monorepo.
echo "The optional Spentd API was moved to: ~/Projects/spentd-api" >&2
echo "See that project's README (and scripts/emulator-api.sh) if you need the server." >&2
exit 1
