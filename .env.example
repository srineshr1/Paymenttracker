# Mobile (optional). The app is local-first and does not need a backend.
# Only set this if you run a separate Spentd API (see ~/Projects/spentd-api).
# Web / desktop browser:
# EXPO_PUBLIC_API_URL=http://localhost:3001
# Android emulator: http://10.0.2.2:3001
# Physical phone:   http://<your-lan-ip>:3001
