# Copy to keystore.properties (gitignored).
# Paths are relative to apps/mobile/android/ once copied there.

storeFile=../credentials/spentd-upload.keystore
storePassword=CHANGE_ME
keyAlias=spentd
keyPassword=CHANGE_ME
